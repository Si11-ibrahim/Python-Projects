import turtle as tl
from turtle import Screen, speed
import turtle

dr = tl.Turtle()
sc = tl.Screen()

tl.title('@Si11_IBRAHIM')
tl.bgcolor('black')
dr.pensize(21)
dr.pencolor('white')

turtle.screensize(852, 508,'black')
     


dr.speed(50)
dr.penup()
dr.left(160)
dr.left(20)
dr.setpos(-285,140)
dr.pendown()
dr.shape('square')


dr.speed(3)
dr.fillcolor('white')
dr.stamp()
dr.forward(70)              # S          
for i in range(60):         # S first Curve
    dr.left(3)
    dr.forward(1.2)

dr.forward(50)
for i in range(60):         # S Second Curve
    dr.right(3)
    dr.forward(1.2)
dr.forward(140)
dr.stamp()

dr.speed(200)
dr.penup()
dr.setpos(-200,140)
dr.pendown()


dr.fillcolor('#d92372')
dr.begin_fill()
for i in range(240):         # Q 
    dr.left(1.5)
    dr.forward(1.2)
dr.end_fill()

dr.fillcolor('white')
dr.penup()
dr.setpos(-200,95)
dr.pendown()

dr.speed(3)
dr.left(120)
dr.forward(105)
dr.stamp()

dr.fillcolor('white')
dr.penup()
dr.setpos(-120,140)
dr.left(60)
dr.pendown()

dr.stamp()
dr.right(90)                # U
dr.forward(50)
dr.speed(500)
for i in range(120):
    dr.left(1.5)
    dr.forward(1.2)

dr.speed(2)
dr.forward(120)
dr.stamp()


dr.penup()
dr.setpos(10,143)
dr.pendown()

dr.stamp()
dr.right(180)               #  I
dr.forward(110)
dr.stamp()


dr.penup()
dr.setpos(50,140)
dr.pendown()

dr.stamp()
dr.forward(105)             #  D
dr.left(90)
dr.forward(80)
dr.speed(500)
for i in range(120):
    dr.left(1.5)
    dr.forward(1.4)

dr.speed(2)
dr.forward(79)
dr.left(90)
dr.forward(2)
dr.right(90)
dr.stamp()

dr.penup()
dr.setpos(-155,-15)
dr.pendown()

dr.shape('square')
dr.fillcolor('white')
dr.stamp()
dr.forward(90)                 #  G
dr.speed(500)
for i in range(120):
    dr.left(1.5)
    dr.forward(1.4)

dr.speed(4)
dr.forward(105)
dr.stamp()



dr.penup()
dr.setpos(-240,-70)
dr.pendown()

dr.stamp()
dr.forward(55)
dr.right(60)
dr.forward(100)
dr.stamp()




dr.penup()
dr.setpos(-100,-125)
dr.pendown()


dr.fillcolor('white')

dr.begin_fill
dr.left(125)                    #  A
dr.forward(115)
dr.right(135)
dr.forward(110)
dr.end_fill

dr.penup()
dr.setpos(30,-125)
dr.pendown()



dr.right(200)                  #  M
dr.stamp()
dr.forward(100)
dr.right(145)
dr.forward(107)
dr.left(110)
dr.forward(104)
dr.right(145)
dr.forward(100)
dr.stamp()


dr.penup()
dr.setpos(300,-125)
dr.pendown()


dr.begin_fill                  #  E
dr.stamp()
dr.right(90)
dr.forward(100)
dr.stamp()
dr.right(90)
dr.forward(100)
dr.stamp()
dr.right(90)
dr.forward(100)
dr.stamp()
dr.end_fill()

dr.penup()
dr.setpos(260,-75)
dr.pendown()

dr.stamp()
dr.forward(100)
dr.stamp()






 
tl.done()


  #            ###################      ##########################           ########                ########
  #            ###################      ##########################          #########               #########
  #            ####                               #####                    #### #####              #### #####
  #            ####                               #####                   ####  #####             ####  #####
  #            ####                               #####                  ####   #####            ####   #####
  #            ###################                #####                         #####                   #####
  #            ###################                #####                         #####                   #####
  #                           ####                #####                         #####                   #####
  #                           ####                #####                         #####                   #####
  #                           ####                #####                         #####                   #####
  #            ###################      ##########################      #####################   #####################
  #            ###################      ##########################      #####################   #####################
